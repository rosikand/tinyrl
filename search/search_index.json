{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"tinyrl","text":"<p>A minimal reinforcement learning toolkit built from scratch. Provides simple RL environments and training utilities for learning and experimenting with RL algorithms.</p>"},{"location":"#why-tinyrl","title":"Why tinyrl?","text":"<ul> <li>Simple \u2014 small codebase, easy to read and modify</li> <li>From scratch \u2014 no dependency on OpenAI Gym or Gymnasium</li> <li>Extensible \u2014 add new environments by subclassing <code>Environment</code>, new policies by subclassing <code>Policy</code></li> <li>Batteries included \u2014 comes with a <code>Runner</code> for rolling out episodes and a <code>TrainingMonitor</code> for plotting training curves</li> </ul>"},{"location":"#quick-example","title":"Quick example","text":"<pre><code>from tinyrl import GridWorld, Runner, RandomPolicy\n\nenv = GridWorld()\nrunner = Runner(env)\npolicy = RandomPolicy(n_actions=env.n_actions)\n\n# run 100 episodes\nfor _ in range(100):\n    result = runner.run_episode(policy)\n\n# plot reward and episode length curves\nrunner.plot()\n</code></pre>"},{"location":"#package-structure","title":"Package structure","text":"<pre><code>tinyrl/\n  core/          # Environment, Policy, Runner, TrainingMonitor, types\n  envs/          # GridWorld, ...\n  algorithms/    # RandomPolicy, ...\n</code></pre>"},{"location":"adding-environments/","title":"Adding Environments","text":"<p>To create a new environment, subclass <code>Environment</code> and implement four methods.</p>"},{"location":"adding-environments/#template","title":"Template","text":"<pre><code>import numpy as np\nfrom tinyrl import Environment\n\n\nclass MyEnv(Environment):\n    def __init__(self):\n        self.state_dim = 4        # observation dimensionality\n        self.n_actions = 2        # for discrete action spaces\n        # self.action_dim = 3     # for continuous action spaces\n        self.max_steps = 200      # episode step limit\n        # ... your state variables ...\n\n    def reset(self) -&gt; np.ndarray:\n        \"\"\"Reset to initial state, return first observation.\"\"\"\n        self.steps = 0\n        # ... reset your state ...\n        return self._get_obs()\n\n    def step(self, action) -&gt; tuple[np.ndarray, float, bool]:\n        \"\"\"Take action, return (observation, reward, done).\"\"\"\n        self.steps += 1\n        # ... apply action, compute reward ...\n        done = self.steps &gt;= self.max_steps  # or some goal condition\n        return self._get_obs(), reward, done\n\n    def _get_obs(self) -&gt; np.ndarray:\n        \"\"\"Return current state as numpy array.\"\"\"\n        # ... build observation vector ...\n        return np.array([...], dtype=np.float32)\n\n    def render(self, action=None, step_num=0):\n        \"\"\"Print or display the current state.\"\"\"\n        print(f\"Step {step_num}: {self._get_obs()}\")\n</code></pre>"},{"location":"adding-environments/#checklist","title":"Checklist","text":"<ol> <li>Set <code>state_dim</code> and <code>max_steps</code> in <code>__init__</code></li> <li>Set <code>n_actions</code> (discrete) or <code>action_dim</code> (continuous)</li> <li><code>reset()</code> returns the initial observation and resets all state</li> <li><code>step()</code> returns <code>(obs, reward, done)</code> \u2014 make sure <code>done=True</code> when the episode should end</li> <li><code>_get_obs()</code> returns a numpy array with shape <code>(state_dim,)</code></li> <li><code>render()</code> displays something useful for debugging</li> </ol>"},{"location":"adding-environments/#using-with-runner","title":"Using with Runner","text":"<p>Once implemented, your environment works with <code>Runner</code> automatically:</p> <pre><code>from tinyrl import Runner\n\nenv = MyEnv()\nrunner = Runner(env)\n\nresult = runner.run_episode(my_policy, visualize=True)\nrunner.plot()\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>pip install git+https://github.com/rosikand/tinyrl.git\n</code></pre> <p>Or clone and install in editable mode for development:</p> <pre><code>git clone https://github.com/rosikand/tinyrl.git\ncd tinyrl\npip install -e .\n</code></pre>"},{"location":"getting-started/#your-first-episode","title":"Your first episode","text":"<pre><code>from tinyrl import GridWorld, Runner, RandomPolicy\n\nenv = GridWorld()\nrunner = Runner(env)\npolicy = RandomPolicy(n_actions=env.n_actions)\n\n# run and visualize one episode\nresult = runner.run_episode(policy, visualize=True)\nprint(f\"Reward: {result.reward}, Steps: {result.steps}\")\n</code></pre>"},{"location":"getting-started/#training-loop","title":"Training loop","text":"<p>A typical training loop looks like this:</p> <pre><code>from tinyrl import GridWorld, Runner\n\nenv = GridWorld()\nrunner = Runner(env)\n\nfor episode in range(500):\n    # replace with your learned policy\n    result = runner.run_episode(my_policy)\n\n    if episode % 100 == 0:\n        print(f\"Episode {episode}: reward={result.reward:.0f}, steps={result.steps}\")\n\nrunner.plot()\n</code></pre> <p>The <code>Runner</code> automatically logs reward, episode length, and policy entropy (if provided) to its internal <code>TrainingMonitor</code>. Call <code>runner.plot()</code> at any point to see training curves.</p>"},{"location":"getting-started/#policy-function","title":"Policy function","text":"<p>The <code>policy_fn</code> passed to <code>run_episode</code> can be:</p> <ul> <li> <p>A plain callable returning an <code>int</code> (discrete) or <code>np.ndarray</code> (continuous):   <pre><code>lambda obs: np.random.randint(4)\n</code></pre></p> </li> <li> <p>A callable returning <code>PolicyOutput</code> with optional logprob and entropy:   <pre><code>from tinyrl import PolicyOutput\n\ndef my_policy(obs):\n    return PolicyOutput(action=2, logprob=-0.5, entropy=1.2)\n</code></pre></p> </li> <li> <p>A <code>Policy</code> subclass for structured, reusable policies:   <pre><code>from tinyrl import Policy, PolicyOutput\n\nclass MyPolicy(Policy):\n    def __call__(self, obs):\n        return PolicyOutput(action=2, entropy=1.2)\n</code></pre></p> </li> </ul>"},{"location":"getting-started/#episoderesult","title":"EpisodeResult","text":"<p><code>run_episode</code> always returns an <code>EpisodeResult</code>:</p> <pre><code>result = runner.run_episode(policy)\nresult.reward       # total episode reward\nresult.steps        # number of steps taken\nresult.trajectory   # None unless return_trajectory=True\n</code></pre>"},{"location":"getting-started/#trajectories","title":"Trajectories","text":"<p>Request the full trajectory to inspect or use for training:</p> <pre><code>result = runner.run_episode(policy, return_trajectory=True)\ntraj = result.trajectory\n\ntraj[0].obs          # first step observation\ntraj.obs             # batched obs array, shape (steps, state_dim)\ntraj.actions         # batched actions\ntraj.rewards         # batched rewards\ntraj.logprobs        # batched logprobs (None if not provided)\ntraj.entropies       # batched entropies (None if not provided)\n</code></pre>"},{"location":"getting-started/#eval-mode","title":"Eval mode","text":"<p>Run episodes without logging to the monitor:</p> <pre><code>result = runner.run_episode(policy, log=False)\n</code></pre>"},{"location":"api/algorithms/","title":"Algorithms","text":"<p>Built-in policy implementations.</p>"},{"location":"api/algorithms/#randompolicy","title":"RandomPolicy","text":"<p>Uniform random policy over discrete actions.</p> <pre><code>from tinyrl import GridWorld, Runner, RandomPolicy\n\nenv = GridWorld()\npolicy = RandomPolicy(n_actions=env.n_actions)\nrunner = Runner(env)\n\nresult = runner.run_episode(policy)\n</code></pre>"},{"location":"api/algorithms/#constructor","title":"Constructor","text":""},{"location":"api/algorithms/#randompolicyn_actions-int","title":"<code>RandomPolicy(n_actions: int)</code>","text":"<p>Args:</p> <ul> <li><code>n_actions</code> \u2014 number of discrete actions to sample from uniformly</li> </ul>"},{"location":"api/environment/","title":"Environment","text":"<p><code>tinyrl.Environment</code> is the abstract base class for all environments. It defines the interface that every environment must implement.</p>"},{"location":"api/environment/#attributes","title":"Attributes","text":"Attribute Type Description <code>state_dim</code> <code>int</code> Dimensionality of the observation vector <code>n_actions</code> <code>int \\| None</code> Number of discrete actions (for discrete envs) <code>action_dim</code> <code>int \\| None</code> Dimensionality of the action vector (for continuous envs) <code>max_steps</code> <code>int</code> Maximum steps per episode <p>Set <code>n_actions</code> for discrete action spaces, <code>action_dim</code> for continuous.</p>"},{"location":"api/environment/#abstract-methods","title":"Abstract methods","text":""},{"location":"api/environment/#reset-npndarray","title":"<code>reset() -&gt; np.ndarray</code>","text":"<p>Reset the environment to its initial state.</p> <p>Returns: the initial observation as a numpy array.</p> <pre><code>obs = env.reset()\n</code></pre>"},{"location":"api/environment/#stepaction-int-npndarray-tuplenpndarray-float-bool","title":"<code>step(action: int | np.ndarray) -&gt; tuple[np.ndarray, float, bool]</code>","text":"<p>Take one step in the environment.</p> <p>Args:</p> <ul> <li><code>action</code> \u2014 action index (int, discrete) or action vector (np.ndarray, continuous)</li> </ul> <p>Returns: a tuple of <code>(observation, reward, done)</code></p> <ul> <li><code>observation</code> \u2014 the new state as a numpy array</li> <li><code>reward</code> \u2014 scalar reward for this transition</li> <li><code>done</code> \u2014 <code>True</code> if the episode has ended</li> </ul> <pre><code>obs, reward, done = env.step(action)\n</code></pre>"},{"location":"api/environment/#_get_obs-npndarray","title":"<code>_get_obs() -&gt; np.ndarray</code>","text":"<p>Return the current observation. Called internally by <code>reset()</code> and <code>step()</code>.</p>"},{"location":"api/environment/#renderactionnone-step_num0","title":"<code>render(action=None, step_num=0)</code>","text":"<p>Display the current state of the environment.</p> <p>Args:</p> <ul> <li><code>action</code> \u2014 the action that was just taken (for display purposes)</li> <li><code>step_num</code> \u2014 current step number in the episode</li> </ul>"},{"location":"api/gridworld/","title":"GridWorld","text":"<p>A simple grid-based MDP. The agent starts at the top-left corner and must navigate to the bottom-right goal.</p> <pre><code>from tinyrl import GridWorld\n\nenv = GridWorld(size=5)\n</code></pre>"},{"location":"api/gridworld/#mdp-specification","title":"MDP specification","text":"Component Value States <code>(row, col)</code> positions, normalized to <code>[0, 1]</code> Actions <code>0</code>=up, <code>1</code>=right, <code>2</code>=down, <code>3</code>=left Transitions Deterministic. Moves in the chosen direction, clipped at walls Rewards <code>-1</code> per step, <code>+10</code> when reaching the goal Start <code>(0, 0)</code> \u2014 top-left Goal <code>(size-1, size-1)</code> \u2014 bottom-right Horizon 50 steps max"},{"location":"api/gridworld/#constructor","title":"Constructor","text":""},{"location":"api/gridworld/#gridworldsize5","title":"<code>GridWorld(size=5)</code>","text":"<p>Args:</p> <ul> <li><code>size</code> \u2014 grid dimension (creates a <code>size x size</code> grid). Default: <code>5</code>.</li> </ul>"},{"location":"api/gridworld/#example","title":"Example","text":"<pre><code>from tinyrl import GridWorld\n\nenv = GridWorld()\nobs = env.reset()          # array([0., 0.])\nobs, r, done = env.step(2) # move down -&gt; array([0.25, 0.]), r=-1.0\nobs, r, done = env.step(1) # move right -&gt; array([0.25, 0.25]), r=-1.0\n</code></pre>"},{"location":"api/gridworld/#optimal-policy","title":"Optimal policy","text":"<p>The shortest path from <code>(0,0)</code> to <code>(4,4)</code> takes 8 steps (4 down + 4 right), giving a total reward of <code>+2</code> (7 steps \u00d7 -1 + 1 step with +10).</p>"},{"location":"api/monitor/","title":"TrainingMonitor","text":"<p>Logs episode statistics and plots training curves. Automatically used by <code>Runner</code>, but can also be used standalone.</p> <pre><code>from tinyrl import TrainingMonitor\n\nmonitor = TrainingMonitor(window=50)\n</code></pre>"},{"location":"api/monitor/#constructor","title":"Constructor","text":""},{"location":"api/monitor/#trainingmonitorwindow50","title":"<code>TrainingMonitor(window=50)</code>","text":"<p>Args:</p> <ul> <li><code>window</code> \u2014 rolling average window size for smoothing plots</li> </ul>"},{"location":"api/monitor/#methods","title":"Methods","text":""},{"location":"api/monitor/#logreward-length-entropynone","title":"<code>log(reward, length, entropy=None)</code>","text":"<p>Record one episode's statistics.</p> <p>Args:</p> <ul> <li><code>reward</code> \u2014 total episode reward</li> <li><code>length</code> \u2014 number of steps in the episode</li> <li><code>entropy</code> \u2014 mean policy entropy over the episode (optional)</li> </ul>"},{"location":"api/monitor/#plot","title":"<code>plot()</code>","text":"<p>Display training curves side by side:</p> <ol> <li>Episode Reward</li> <li>Episode Length</li> <li>Policy Entropy \u2014 only shown if entropy data was logged</li> </ol> <p>Each plot shows the raw data (transparent) and a rolling average (solid line).</p>"},{"location":"api/monitor/#attributes","title":"Attributes","text":"Attribute Type Description <code>rewards</code> <code>list[float]</code> All logged episode rewards <code>lengths</code> <code>list[int]</code> All logged episode lengths <code>entropies</code> <code>list[float]</code> Logged mean entropies (only for episodes that provided entropy)"},{"location":"api/monitor/#standalone-usage","title":"Standalone usage","text":"<pre><code>from tinyrl import TrainingMonitor\n\nmonitor = TrainingMonitor()\n\nfor episode in range(500):\n    # ... your training loop ...\n    monitor.log(total_reward, steps, entropy)  # entropy is optional\n\nmonitor.plot()\n</code></pre>"},{"location":"api/policy/","title":"Policy","text":"<p><code>tinyrl.Policy</code> is the abstract base class for policies. Subclass it to create reusable, structured policies that work with <code>Runner.run_episode()</code>.</p> <pre><code>from tinyrl import Policy, PolicyOutput\n\nclass MyPolicy(Policy):\n    def __call__(self, obs):\n        action = ...\n        return PolicyOutput(action=action, logprob=-0.5, entropy=1.2)\n</code></pre> <p>Using the <code>Policy</code> ABC is optional \u2014 <code>run_episode</code> also accepts any callable. The ABC is useful when building real policies because it provides a clear interface and catches missing implementations at instantiation time.</p>"},{"location":"api/policy/#abstract-methods","title":"Abstract methods","text":""},{"location":"api/policy/#__call__obs-npndarray-int-npndarray-policyoutput","title":"<code>__call__(obs: np.ndarray) -&gt; int | np.ndarray | PolicyOutput</code>","text":"<p>Choose an action given an observation.</p> <p>Args:</p> <ul> <li><code>obs</code> \u2014 current observation from the environment</li> </ul> <p>Returns: one of:</p> <ul> <li><code>int</code> \u2014 discrete action index</li> <li><code>np.ndarray</code> \u2014 continuous action vector</li> <li><code>PolicyOutput</code> \u2014 action with optional logprob and entropy</li> </ul>"},{"location":"api/runner/","title":"Runner","text":"<p>The <code>Runner</code> manages the episode loop, connecting an environment with a policy and an optional training monitor.</p> <pre><code>from tinyrl import GridWorld, Runner\n\nenv = GridWorld()\nrunner = Runner(env)\n</code></pre>"},{"location":"api/runner/#constructor","title":"Constructor","text":""},{"location":"api/runner/#runnerenv-monitornone","title":"<code>Runner(env, monitor=None)</code>","text":"<p>Args:</p> <ul> <li><code>env</code> \u2014 an <code>Environment</code> instance</li> <li><code>monitor</code> \u2014 a <code>TrainingMonitor</code> instance. If <code>None</code>, a default one is created automatically.</li> </ul>"},{"location":"api/runner/#methods","title":"Methods","text":""},{"location":"api/runner/#run_episodepolicy_fn-visualizefalse-delay01-logtrue-return_trajectoryfalse","title":"<code>run_episode(policy_fn, visualize=False, delay=0.1, log=True, return_trajectory=False)</code>","text":"<p>Roll out a single episode.</p> <p>Args:</p> <ul> <li><code>policy_fn</code> \u2014 a callable that takes an observation and returns either:<ul> <li><code>int</code> or <code>np.ndarray</code> \u2014 the action to take</li> <li><code>PolicyOutput</code> \u2014 action plus optional logprob and entropy</li> </ul> </li> <li><code>visualize</code> \u2014 if <code>True</code>, calls <code>env.render()</code> at each step</li> <li><code>delay</code> \u2014 seconds between frames when visualizing</li> <li><code>log</code> \u2014 if <code>True</code>, logs stats to the monitor. Set <code>False</code> for eval runs.</li> <li><code>return_trajectory</code> \u2014 if <code>True</code>, populates <code>result.trajectory</code></li> </ul> <p>Returns: <code>EpisodeResult</code></p> <pre><code># basic\nresult = runner.run_episode(policy)\nresult.reward       # total reward\nresult.steps        # number of steps\n\n# with trajectory\nresult = runner.run_episode(policy, return_trajectory=True)\nresult.trajectory   # Trajectory object\n\n# eval (no logging)\nresult = runner.run_episode(policy, log=False)\n</code></pre>"},{"location":"api/runner/#plot","title":"<code>plot()</code>","text":"<p>Calls <code>self.monitor.plot()</code> to display training curves.</p> <pre><code>for _ in range(500):\n    runner.run_episode(policy)\nrunner.plot()\n</code></pre>"},{"location":"api/types/","title":"Types","text":"<p>Data classes used throughout tinyrl.</p>"},{"location":"api/types/#policyoutput","title":"PolicyOutput","text":"<p>Structured return type for policy functions.</p> <pre><code>from tinyrl import PolicyOutput\n\n# just an action\nPolicyOutput(action=2)\n\n# with training info\nPolicyOutput(action=2, logprob=-0.3, entropy=1.2)\n</code></pre> Field Type Default Description <code>action</code> <code>int \\| np.ndarray</code> required The action to take <code>logprob</code> <code>float \\| None</code> <code>None</code> Log-probability of the action <code>entropy</code> <code>float \\| None</code> <code>None</code> Entropy of the policy distribution"},{"location":"api/types/#step","title":"Step","text":"<p>A single environment transition, stored in trajectories.</p> Field Type Default Description <code>obs</code> <code>np.ndarray</code> required Observation before the action <code>action</code> <code>int \\| np.ndarray</code> required Action taken <code>reward</code> <code>float</code> required Reward received <code>next_obs</code> <code>np.ndarray</code> required Observation after the action <code>done</code> <code>bool</code> required Whether the episode ended <code>logprob</code> <code>float \\| None</code> <code>None</code> Log-probability (if policy provided it) <code>entropy</code> <code>float \\| None</code> <code>None</code> Entropy (if policy provided it)"},{"location":"api/types/#trajectory","title":"Trajectory","text":"<p>A full episode trajectory. Supports indexing, iteration, and batched property access.</p> <pre><code>result = runner.run_episode(policy, return_trajectory=True)\ntraj = result.trajectory\n\ntraj[0]            # first Step\nlen(traj)          # number of steps\nfor step in traj:  # iterate\n    ...\n</code></pre>"},{"location":"api/types/#batched-properties","title":"Batched properties","text":"Property Shape Description <code>traj.obs</code> <code>(length, state_dim)</code> All observations <code>traj.actions</code> <code>(length,)</code> or <code>(length, action_dim)</code> All actions <code>traj.rewards</code> <code>(length,)</code> All rewards <code>traj.logprobs</code> <code>(length,)</code> or <code>None</code> All log-probs (if provided) <code>traj.entropies</code> <code>(length,)</code> or <code>None</code> All entropies (if provided)"},{"location":"api/types/#episoderesult","title":"EpisodeResult","text":"<p>Return type for <code>Runner.run_episode()</code>.</p> Field Type Description <code>reward</code> <code>float</code> Total episode reward <code>steps</code> <code>int</code> Number of steps taken <code>trajectory</code> <code>Trajectory \\| None</code> Full trajectory (if <code>return_trajectory=True</code>)"}]}